please download the dataset from here :

https://www.kaggle.com/competitions/asl-signs.

our project depinds on pixels AKA images so make sure to install tensorflow library so the program work

use (pip install tensorflow)
if that didnt work use (pythin -m pip install tensorflow) or (py -m pip install tensorflow)

